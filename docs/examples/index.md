# Examples

Every script in `examples/` runs on data it generates itself, prints what it found, and is
executed by the test suite, so none of it can drift away from the code.

```bash
uv run python examples/01_least_squares.py
```
